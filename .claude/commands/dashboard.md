---
name: dashboard
description: Generate the factory observability dashboard from current state files
---

Run `.claude/skills/factory-dashboard.md` to generate a human-readable dashboard report.

Read all factory state files, compute metrics and trends, and write the dashboard to `Docs/reports/factory-dashboard.md`.

Display the dashboard content to the user after writing it.
